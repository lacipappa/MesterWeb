const buttons=[...document.querySelectorAll('.lang')];async function setLanguage(lang){try{const response=await fetch(`lang/${lang}.json`);if(!response.ok)throw new Error('Language file not found');const data=await response.json();document.querySelectorAll('[data-i18n]').forEach(el=>{if(data[el.dataset.i18n])el.textContent=data[el.dataset.i18n]});document.documentElement.lang=lang;localStorage.setItem('alma-serena-lang',lang);buttons.forEach(b=>b.classList.toggle('active',b.dataset.lang===lang))}catch(error){console.error(error)}}buttons.forEach(b=>b.addEventListener('click',()=>setLanguage(b.dataset.lang)));const menu=document.querySelector('.menu-button'),nav=document.querySelector('.nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.getElementById('year').textContent=new Date().getFullYear();setLanguage(localStorage.getItem('alma-serena-lang')||'es');
